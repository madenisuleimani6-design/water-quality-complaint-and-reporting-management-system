/** Visible tab bar row height (icons + labels), excluding device bottom inset. */
export const TAB_BAR_CONTENT_HEIGHT = 56;

/** Content area top padding below ScreenHeader. */
export const CONTENT_SHEET_TOP_PADDING = 16;
